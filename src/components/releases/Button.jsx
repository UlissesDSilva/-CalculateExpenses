import React from 'react'
import './Releases.css'

export default props => {

    return(
        <button className='Releases' onClick={props.calculation}>
            Calcule
        </button>
    )
}